(function(){
'use strict';
if(window.__KG_ACCOUNT_DEALER_REQUESTS__)return;
window.__KG_ACCOUNT_DEALER_REQUESTS__=true;

var BUCKET='dealer-sell-images';
var TABLE='dealer_sell_requests';
var PHOTO_TABLE='dealer_sell_request_photos';
var api=null,client=null,user=null,rows=[];

function q(s,r){return (r||document).querySelector(s);}
function qa(s,r){return Array.prototype.slice.call((r||document).querySelectorAll(s));}
function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,function(c){return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c];});}
function money(v){var n=Number(v||0);return n?n.toLocaleString('tr-TR')+' TL':'—';}
function date(v){try{return new Date(v).toLocaleString('tr-TR',{day:'2-digit',month:'2-digit',year:'numeric',hour:'2-digit',minute:'2-digit'});}catch(e){return '—';}}
function statusLabel(v){return {draft:'Taslak',open:'Teklif bekliyor',offer_received:'Teklif geldi',accepted:'Teklif kabul edildi',completed:'Satış tamamlandı',cancelled:'İptal edildi'}[v]||v||'—';}
function statusClass(v){return (v==='open'||v==='offer_received'||v==='accepted'||v==='completed')?'':' off';}
function offerStatusLabel(v){return {active:'Aktif teklif',accepted:'Kabul edildi',rejected:'Başka teklif seçildi',expired:'Süresi doldu',withdrawn:'Geri çekildi'}[v]||v||'—';}
function damageLabel(v){return v?'Hasar beyan edildi':'Görünür hasar yok';}
function warrantyLabel(v){return {var:'Garanti devam ediyor',yok:'Garanti yok',bilmiyorum:'Garanti bilinmiyor'}[v]||'—';}
function boxLabel(v){return {ikisi:'Kutu ve fatura var',kutu:'Sadece kutu var',fatura:'Sadece fatura var',yok:'Kutu / fatura yok'}[v]||'—';}
function photoTypeLabel(v){return {front:'Ön yüz',back:'Arka yüz',side:'Yan / kasa',corners:'Köşeler / yakın çekim',damage:'Hasar detayı'}[v]||v;}
function remaining(v){var ms=new Date(v).getTime()-Date.now();if(!Number.isFinite(ms)||ms<=0)return 'Süresi doldu';var h=Math.floor(ms/3600000),m=Math.max(1,Math.floor((ms%3600000)/60000));return h>0?h+' sa '+m+' dk kaldı':m+' dk kaldı';}
function telHref(v){return 'tel:'+String(v||'').replace(/[^0-9+]/g,'');}

function ensureStyle(){
 if(q('#kgDealerAccountStyle'))return;
 var s=document.createElement('style');s.id='kgDealerAccountStyle';s.textContent=''
 +'.kg-dealer-account-intro{padding:12px 18px;background:#f0fff6;border-bottom:1px solid #d9f2e3;color:#176b3a;font-size:11px;line-height:1.5}'
 +'.kg-dealer-modal{position:fixed;inset:0;z-index:1000020;background:rgba(7,20,38,.72);display:flex;align-items:center;justify-content:center;padding:18px;box-sizing:border-box}'
 +'.kg-dealer-modal-card{width:min(900px,100%);max-height:92vh;overflow:auto;background:#fff;border-radius:18px;box-shadow:0 28px 80px rgba(2,6,23,.35);padding:22px;box-sizing:border-box;color:#172033;position:relative}'
 +'.kg-dealer-modal-close{position:absolute;right:14px;top:14px;width:36px;height:36px;border:0;border-radius:10px;background:#f2f4f7;font-size:20px;cursor:pointer}'
 +'.kg-dealer-modal-card h2{margin:0 44px 5px 0;font-size:22px}.kg-dealer-modal-sub{color:#667085;font-size:12px;margin-bottom:15px}'
 +'.kg-dealer-detail-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px;margin:14px 0}.kg-dealer-detail-item{padding:11px;border:1px solid #e1e7ef;border-radius:11px;background:#f8fafc}.kg-dealer-detail-item span{display:block;color:#7b8798;font-size:10px}.kg-dealer-detail-item strong{display:block;margin-top:4px;font-size:12px}'
 +'.kg-dealer-photos{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px;margin-top:12px}.kg-dealer-photo{overflow:hidden;border:1px solid #e1e7ef;border-radius:12px;background:#f8fafc}.kg-dealer-photo img{display:block;width:100%;aspect-ratio:1.15;object-fit:cover;background:#eef2f6}.kg-dealer-photo span{display:block;padding:8px 10px;font-size:10px;font-weight:850;color:#475467}'
 +'.kg-dealer-modal-note{margin-top:14px;padding:11px 12px;border-radius:10px;background:#f0fff6;color:#176b3a;font-size:11px;line-height:1.5}.kg-dealer-modal-actions{display:flex;justify-content:flex-end;gap:8px;margin-top:16px}.kg-dealer-modal-actions button{min-height:39px;border:1px solid #dfe5ee;border-radius:10px;background:#fff;padding:0 13px;font-weight:850;cursor:pointer}.kg-dealer-modal-actions .danger{color:#b42318}.kg-dealer-request-code{font-size:10px;color:#667085;margin-top:4px}'
 +'.kg-offer-list{display:grid;gap:10px;margin-top:10px}.kg-offer-card{border:1px solid #e1e7ef;border-radius:13px;padding:14px;background:#fff;display:grid;grid-template-columns:minmax(0,1fr) auto;gap:12px;align-items:center}.kg-offer-card.accepted{border-color:#9fddb9;background:#f2fff7}.kg-offer-card.inactive{background:#f8fafc;opacity:.78}.kg-offer-main strong{display:block;font-size:13px}.kg-offer-main span{display:block;margin-top:4px;color:#667085;font-size:10.5px;line-height:1.45}.kg-offer-price{text-align:right}.kg-offer-price strong{display:block;font-size:20px;color:#087a37}.kg-offer-price span{display:block;margin-top:3px;font-size:10px;color:#667085}.kg-offer-actions{grid-column:1/-1;display:flex;justify-content:flex-end;gap:7px}.kg-offer-actions button{min-height:36px;border:0;border-radius:9px;padding:0 12px;font-size:10.5px;font-weight:900;cursor:pointer;background:#10b956;color:#fff}.kg-offer-contact{margin-top:12px;border:1px solid #a7dfbd;border-radius:12px;background:#effcf4;padding:13px}.kg-offer-contact h4{margin:0 0 8px;font-size:13px;color:#176b3a}.kg-offer-contact-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px}.kg-offer-contact-grid div{background:#fff;border:1px solid #d7eee1;border-radius:9px;padding:9px}.kg-offer-contact-grid span{display:block;color:#667085;font-size:9.5px}.kg-offer-contact-grid strong,.kg-offer-contact-grid a{display:block;margin-top:3px;font-size:11px;font-weight:850;color:#172033;text-decoration:none}.kg-offer-warning{margin-top:8px;color:#8a5b08;font-size:10px;line-height:1.45}'
 +'@media(max-width:720px){.kg-dealer-detail-grid{grid-template-columns:1fr 1fr}.kg-dealer-photos{grid-template-columns:1fr 1fr}.kg-dealer-modal-card{padding:18px}.kg-offer-card{grid-template-columns:1fr}.kg-offer-price{text-align:left}.kg-offer-actions{justify-content:flex-start}.kg-offer-contact-grid{grid-template-columns:1fr}}@media(max-width:480px){.kg-dealer-detail-grid,.kg-dealer-photos{grid-template-columns:1fr}.kg-dealer-modal{padding:10px}.kg-dealer-modal-card{max-height:calc(100dvh - 20px)}}';
 document.head.appendChild(s);
}

function ensureUI(){
 ensureStyle();
 var stats=q('.kg-account-stats');
 if(stats&&!q('#kgCountDealerRequests')){var stat=document.createElement('div');stat.className='kg-account-stat';stat.innerHTML='<span>Telefoncuya Sat</span><strong id="kgCountDealerRequests">0</strong><small>Teklif taleplerin</small>';stats.appendChild(stat);}
 var tabs=q('.kg-account-tabs');
 if(tabs&&!q('[data-tab="dealer-requests"]',tabs)){var btn=document.createElement('button');btn.type='button';btn.className='kg-account-tabbtn';btn.dataset.tab='dealer-requests';btn.textContent='Telefoncuya Sat';tabs.appendChild(btn);btn.addEventListener('click',function(){qa('.kg-account-tabbtn').forEach(function(x){x.classList.toggle('active',x===btn);});qa('.kg-account-panel').forEach(function(p){p.classList.toggle('active',p.dataset.panel==='dealer-requests');});load();});}
 var content=q('#kgAccountContent');
 if(content&&!q('[data-panel="dealer-requests"]',content)){var sec=document.createElement('section');sec.className='kg-account-panel';sec.dataset.panel='dealer-requests';sec.innerHTML='<div class="kg-account-card"><div class="kg-account-cardhead"><h2>Telefoncuya Sat Taleplerim</h2><span>Cihazın için açtığın alış teklif talepleri</span></div><div class="kg-dealer-account-intro">Fotoğrafların herkese açık değildir. Yalnızca doğrulanmış telefoncular teklif sürecinde görebilir. İletişim bilgilerin ise sen bir teklifi kabul edene kadar gizli kalır.</div><div id="kgDealerRequestList" class="kg-account-list"><div class="kg-account-loading">Talepler yükleniyor…</div></div></div>';content.appendChild(sec);}
}

async function waitBackend(tries){tries=tries||0;if(window.KGMarketplaceSupabase)return window.KGMarketplaceSupabase;if(tries>120)throw new Error('Üyelik sistemi yüklenemedi.');await new Promise(function(r){setTimeout(r,50);});return waitBackend(tries+1);}

async function load(){
 if(!user||!client)return;
 var box=q('#kgDealerRequestList');if(!box)return;
 box.innerHTML='<div class="kg-account-loading">Talepler yükleniyor…</div>';
 var r=await client.from(TABLE).select('id,request_code,brand,model,storage,market_value,city,district,battery,warranty,box_invoice,notes,has_damage,status,created_at,updated_at').eq('user_id',user.id).order('created_at',{ascending:false}).limit(50);
 if(r.error){box.innerHTML='<div class="kg-account-empty"><strong>Talepler yüklenemedi</strong>Lütfen sayfayı yenileyip tekrar dene.</div>';return;}
 rows=r.data||[];var count=q('#kgCountDealerRequests');if(count)count.textContent=String(rows.length);
 if(!rows.length){box.innerHTML='<div class="kg-account-empty"><strong>Henüz teklif talebin yok</strong>Bir cihazın değerini hesapladıktan sonra “Telefoncuya Sat” ile telefonculardan teklif talebi oluşturabilirsin.</div>';return;}
 box.innerHTML=rows.map(function(x){var canCancel=x.status==='open'||x.status==='offer_received';var buttonLabel=x.status==='offer_received'?'Teklifleri Gör':x.status==='accepted'?'Kabul Edilen Teklif':'Detayları Gör';return '<div class="kg-account-row"><div class="kg-account-row-main"><strong>'+esc(x.brand+' '+x.model)+'</strong><span>'+esc([x.storage,x.city&&x.district?x.city+' / '+x.district:x.city].filter(Boolean).join(' • '))+' • '+esc(date(x.created_at))+'</span><div class="kg-dealer-request-code">Talep No: '+esc(x.request_code)+'</div></div><div class="kg-account-price"><strong>'+esc(money(x.market_value))+'</strong><span>KaçaGider piyasa değeri</span></div><div class="kg-account-meta-wrap"><span class="kg-account-status'+statusClass(x.status)+'">'+esc(statusLabel(x.status))+'</span><span class="kg-account-meta">'+esc(damageLabel(x.has_damage))+'</span></div><div class="kg-account-actions"><button type="button" class="primary" data-dealer-detail="'+esc(x.id)+'">'+esc(buttonLabel)+'</button>'+(canCancel?'<button type="button" class="danger" data-dealer-cancel="'+esc(x.id)+'">Talebi İptal Et</button>':'')+'</div></div>';}).join('');
 qa('[data-dealer-detail]',box).forEach(function(b){b.onclick=function(){var x=rows.find(function(r){return r.id===b.dataset.dealerDetail;});if(x)openDetail(x);};});
 qa('[data-dealer-cancel]',box).forEach(function(b){b.onclick=async function(){var x=rows.find(function(r){return r.id===b.dataset.dealerCancel;});if(!x||!confirm('Bu telefoncu teklif talebini iptal etmek istiyor musun?'))return;b.disabled=true;var u=await client.from(TABLE).update({status:'cancelled',updated_at:new Date().toISOString()}).eq('id',x.id).eq('user_id',user.id);if(u.error){alert('Talep iptal edilemedi. Lütfen tekrar dene.');b.disabled=false;return;}await load();};});
}

async function signedPhotos(requestId){var r=await client.from(PHOTO_TABLE).select('photo_type,object_path,sort_order').eq('request_id',requestId).eq('user_id',user.id).order('sort_order',{ascending:true});if(r.error)throw r.error;var list=r.data||[],out=[];for(var i=0;i<list.length;i++){var s=await client.storage.from(BUCKET).createSignedUrl(list[i].object_path,900);if(!s.error&&s.data&&s.data.signedUrl)out.push({type:list[i].photo_type,url:s.data.signedUrl});}return out;}
async function getOffers(requestId){var r=await client.rpc('seller_list_request_offers',{p_request_id:requestId});if(r.error)throw r.error;return r.data||[];}
async function getAcceptedContact(requestId){var r=await client.rpc('seller_get_accepted_dealer_contact',{p_request_id:requestId});if(r.error)throw r.error;return r.data&&r.data[0]?r.data[0]:null;}

function offerCardHtml(o,x){var inactive=o.offer_status!=='active'&&o.offer_status!=='accepted';var canAccept=o.offer_status==='active'&&!o.is_expired&&(x.status==='open'||x.status==='offer_received');return '<div class="kg-offer-card '+(o.offer_status==='accepted'?'accepted ':'')+(inactive?'inactive':'')+'"><div class="kg-offer-main"><strong>✓ Doğrulanmış Telefoncu · '+esc(o.dealer_store_name)+'</strong><span>'+esc([o.dealer_city,o.dealer_district].filter(Boolean).join(' / '))+' · '+esc(offerStatusLabel(o.offer_status))+'</span>'+(o.note?'<span><strong style="display:inline;font-size:10.5px">Not:</strong> '+esc(o.note)+'</span>':'')+'</div><div class="kg-offer-price"><strong>'+esc(money(o.amount))+'</strong><span>'+(o.offer_status==='active'?esc(remaining(o.valid_until)):esc(offerStatusLabel(o.offer_status)))+'</span></div>'+(canAccept?'<div class="kg-offer-actions"><button type="button" data-accept-offer="'+esc(o.offer_id)+'" data-offer-amount="'+esc(o.amount)+'" data-store="'+esc(o.dealer_store_name)+'">Teklifi Kabul Et</button></div>':'')+'</div>';}
function contactHtml(c){if(!c)return '';return '<div class="kg-offer-contact"><h4>✓ Teklif kabul edildi — iletişim bilgileri açıldı</h4><div class="kg-offer-contact-grid"><div><span>Telefoncu</span><strong>'+esc(c.store_name)+'</strong></div><div><span>Telefon</span><a href="'+esc(telHref(c.phone))+'">'+esc(c.phone||'—')+'</a></div><div><span>Konum</span><strong>'+esc([c.city,c.district].filter(Boolean).join(' / '))+'</strong></div><div><span>Mağaza adresi</span><strong>'+esc(c.address||'—')+'</strong></div><div><span>Kabul edilen teklif</span><strong>'+esc(money(c.amount))+'</strong></div><div><span>Kabul zamanı</span><strong>'+esc(date(c.accepted_at))+'</strong></div></div><div class="kg-offer-warning">Teklif cihazın fiziksel kontrolünden sonra kesinleşir. Cihaz bilgilerinde veya fotoğraflarda beyan edilmeyen bir durum varsa telefoncu teklifini yeniden değerlendirebilir.</div></div>';}

async function renderOffers(root,x){var box=q('#kgDealerOfferList',root);if(!box)return;try{var offers=await getOffers(x.id);if(!offers.length){box.innerHTML='<div class="kg-account-empty"><strong>Henüz teklif yok</strong>Doğrulanmış telefonculardan teklif geldiğinde burada göreceksin.</div>';return;}box.innerHTML=offers.map(function(o){return offerCardHtml(o,x);}).join('');if(x.status==='accepted'){try{var contact=await getAcceptedContact(x.id);if(contact)box.insertAdjacentHTML('beforeend',contactHtml(contact));}catch(_e){}}
 qa('[data-accept-offer]',box).forEach(function(b){b.onclick=async function(){var amount=Number(b.dataset.offerAmount||0),store=b.dataset.store||'telefoncu';if(!confirm(store+' mağazasının '+money(amount)+' teklifini kabul etmek istiyor musun? Kabul ettiğinde diğer teklifler kapanacak ve iletişim bilgileriniz iki tarafa açılacak.'))return;b.disabled=true;b.textContent='Kabul ediliyor…';var r=await client.rpc('seller_accept_offer',{p_offer_id:b.dataset.acceptOffer});if(r.error){alert(r.error.message==='OFFER_EXPIRED'?'Bu teklifin süresi dolmuş.':(r.error.message||'Teklif kabul edilemedi.'));b.disabled=false;b.textContent='Teklifi Kabul Et';return;}root.remove();await load();var fresh=rows.find(function(v){return v.id===x.id;});if(fresh)openDetail(fresh);};});
 }catch(e){box.innerHTML='<div class="kg-account-empty"><strong>Teklifler yüklenemedi</strong>'+esc(e.message||'Lütfen tekrar dene.')+'</div>';}}

async function openDetail(x){
 var o=document.createElement('div');o.className='kg-dealer-modal';o.innerHTML='<section class="kg-dealer-modal-card"><button type="button" class="kg-dealer-modal-close" aria-label="Kapat">×</button><h2>'+esc(x.brand+' '+x.model+(x.storage?' '+x.storage:''))+'</h2><div class="kg-dealer-modal-sub">Talep No: '+esc(x.request_code)+' · '+esc(date(x.created_at))+'</div><div class="kg-dealer-detail-grid"><div class="kg-dealer-detail-item"><span>Durum</span><strong>'+esc(statusLabel(x.status))+'</strong></div><div class="kg-dealer-detail-item"><span>KaçaGider piyasa değeri</span><strong>'+esc(money(x.market_value))+'</strong></div><div class="kg-dealer-detail-item"><span>Konum</span><strong>'+esc([x.city,x.district].filter(Boolean).join(' / '))+'</strong></div><div class="kg-dealer-detail-item"><span>Hasar beyanı</span><strong>'+esc(damageLabel(x.has_damage))+'</strong></div><div class="kg-dealer-detail-item"><span>Pil sağlığı</span><strong>'+(x.battery?esc(x.battery+'%'):'—')+'</strong></div><div class="kg-dealer-detail-item"><span>Garanti</span><strong>'+esc(warrantyLabel(x.warranty))+'</strong></div><div class="kg-dealer-detail-item"><span>Kutu / fatura</span><strong>'+esc(boxLabel(x.box_invoice))+'</strong></div><div class="kg-dealer-detail-item"><span>Not</span><strong>'+esc(x.notes||'—')+'</strong></div></div><h3 style="margin:18px 0 8px;font-size:15px">Yüklediğin fotoğraflar</h3><div id="kgDealerPhotoGrid" class="kg-dealer-photos"><div class="kg-account-loading">Fotoğraflar yükleniyor…</div></div><h3 style="margin:20px 0 8px;font-size:15px">Telefoncu teklifleri</h3><div id="kgDealerOfferList" class="kg-offer-list"><div class="kg-account-loading">Teklifler yükleniyor…</div></div><div class="kg-dealer-modal-note">KaçaGider alış fiyatı belirlemez. Telefoncular kendi tekliflerini verir. İletişim bilgilerin yalnızca sen bir teklifi kabul ettiğinde açılır.</div><div class="kg-dealer-modal-actions"><button type="button" data-close-detail>Kapat</button></div></section>';document.body.appendChild(o);
 function close(){o.remove();}
 q('.kg-dealer-modal-close',o).onclick=close;q('[data-close-detail]',o).onclick=close;o.onclick=function(e){if(e.target===o)close();};
 var grid=q('#kgDealerPhotoGrid',o);
 signedPhotos(x.id).then(function(photos){grid.innerHTML=photos.length?photos.map(function(p){return '<div class="kg-dealer-photo"><img src="'+esc(p.url)+'" alt="'+esc(photoTypeLabel(p.type))+'"><span>'+esc(photoTypeLabel(p.type))+'</span></div>';}).join(''):'<div class="kg-account-empty"><strong>Fotoğraf bulunamadı</strong>Bu talep için kayıtlı fotoğraf yok.</div>';}).catch(function(){grid.innerHTML='<div class="kg-account-empty"><strong>Fotoğraflar yüklenemedi</strong>Lütfen tekrar dene.</div>';});
 renderOffers(o,x);
}

async function boot(){try{ensureUI();api=await waitBackend(0);await api.ready;user=await api.getUser();if(!user)return;client=await api.init();ensureUI();await load();if(client&&client.auth&&typeof client.auth.onAuthStateChange==='function')client.auth.onAuthStateChange(function(event,session){if(event==='SIGNED_IN'&&session&&session.user){user=session.user;load();}});}catch(e){console.warn('KaçaGider Telefoncuya Sat hesap paneli:',e);}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();